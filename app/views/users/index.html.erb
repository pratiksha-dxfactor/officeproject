<h1>Users</h1>

<ul>
  <% @users.each do |user| %>
    <li>
      <a href="<%= user_path(user) %>">
        <%= user.first_name%> <%= user.last_name%>
      </a>
    </li>
  <% end %>
</ul>

<%= link_to "New user", new_user_path %>
